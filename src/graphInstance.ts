import { WorkerClient } from "./engine/workerClient";

export const graph = new WorkerClient();

// Default graph initialization
// graph.setCellInput("A1", "Example");
// graph.setCellInput("B1", "=100");
